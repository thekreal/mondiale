<% if logged_in? %>
  window.location = "<%= user_path(current_user) %>"

<% else %>
  $('[class*="alert"]').remove();
  var obj = $('<%= j render "alerts/flash_message" %>');
  obj.insertBefore('.form-title');
  alertCloseBtn(obj);
<% end %>
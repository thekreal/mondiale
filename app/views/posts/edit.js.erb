var form = $('#post.form-float');
var blurBackground = addBlur('body');
form = $('<%= j render "posts/form", title: "Update Post", submit: "Update" %>');
slideDownShow(form);
clickToClose(blurBackground.add('.close'), form);
var form = $('#chapter.form');
var blurBackground = addBlur('body');
form = $('<%= j render "chapters/form", title: "New Chapter", submit: "Create" %>');
slideDownShow(form);
clickToClose(blurBackground.add('.close'), form);
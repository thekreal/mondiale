var form = $('#chapter.form-float');
var blurBackground = addBlur('body');
form = $('<%= j render "chapters/form", title: "Update Chapter", submit: "Update" %>');
slideDownShow(form);
clickToClose(blurBackground.add('.close'), form);
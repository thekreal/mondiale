<div class="large-6 columns">
	<h1 class="title"><%= link_to trip_plan.title, trip_plan, class: "link" %></h1>

	<% if trip_plan.plan_items.any? %>

			<div class="trip-plan-partial-map">
        <div class="googleMap cover-map" data-objects="<%= plan_items_in_json(trip_plan.plan_items) %>"></div>
      </div>

  <% end %>
</div>
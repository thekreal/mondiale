<% if params[:search] && params[:from] == "welcome" %>
  $('.trips-index').html($("<%= j render 'templates/search_results', objects: @trips %>"));
  coverPhoto();
  loadMaps();
  searchResult();
<% elsif params[:search] %>
  $('.trips-index').html($("<%= j render @trips %>"));
  coverPhoto();
  loadMaps();
<% end %>